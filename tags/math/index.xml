<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Math on Serendipity&#39;s Blog</title>
        <link>https://serendipityerr.github.io/tags/math/</link>
        <description>Recent content in Math on Serendipity&#39;s Blog</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <lastBuildDate>Fri, 17 Jul 2026 23:02:04 +0800</lastBuildDate><atom:link href="https://serendipityerr.github.io/tags/math/index.xml" rel="self" type="application/rss+xml" /><item>
        <title>Study Notes on Forward and Reverse KL Divergence</title>
        <link>https://serendipityerr.github.io/p/study-notes-on-forward-and-reverse-kl-divergence/</link>
        <pubDate>Fri, 17 Jul 2026 23:02:04 +0800</pubDate>
        
        <guid>https://serendipityerr.github.io/p/study-notes-on-forward-and-reverse-kl-divergence/</guid>
        <description>&lt;h2 id=&#34;introduction-to-kl-divergence&#34;&gt;Introduction to KL Divergence
&lt;/h2&gt;&lt;h3 id=&#34;definition&#34;&gt;Definition
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Kullback-Leibler (KL) Divergence&lt;/strong&gt; (also known as &lt;strong&gt;Relative Entropy&lt;/strong&gt;) is a metric used to measure the difference between two probability distributions.&lt;/p&gt;
&lt;p&gt;As for two discrete probability distributions $P$ and $Q$ defined on the same probability space, the KL Divergence is defined as:&lt;/p&gt;
$$
D_{\text{KL}}(P \ \Vert \ Q) = \sum_{x \in \mathcal{X}} P(x) \log \frac{P(x)}{Q(x)}.
$$&lt;p&gt;As for two continuous probability distributions $p$ and $q$, the KL Divergence is defined as:&lt;/p&gt;
$$
D_{\text{KL}}(p \ \Vert \ q) = \int_{\mathcal{X}} p(x) \log \frac{p(x)}{q(x)} \mathrm{d} x = \mathbb{E}_{x \sim p(x)} \left[ \log \frac{p(x)}{q(x)} \right].
$$&lt;p&gt;In the following sections, we mainly focus on the continuous version.&lt;/p&gt;
&lt;h3 id=&#34;key-properties&#34;&gt;Key Properties
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Non-negativity (Gibbs&amp;rsquo; Inequality)&lt;/strong&gt;: $D_{\text{KL}}(p \ \Vert \ q) \geq 0$ and the equality holds iff $p(x) = q(x)$ (almost anywhere).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Asymmetry&lt;/strong&gt;: Generally, $D_{\text{KL}}(p \ \Vert \ q) \neq D_{\text{KL}}(q \ \Vert \ p)$. Therefore, the KL Divergence is &lt;strong&gt;not&lt;/strong&gt; a true distance metric.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;forward-kl-vs-reverse-kl&#34;&gt;Forward KL v.s. Reverse KL
&lt;/h2&gt;&lt;p&gt;In Machine Learning, we often aim to minimize the discrepancy between a true distribution $p(x)$ and a model distribution $q_\theta(x)$. The optimization objective varies depending on which distribution is placed in the expectation operator. Define the two optimization objectives:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Forward KL Divergence: $\min_{q_ \theta} D_{\text{KL}}(p \ \Vert \ q_ \theta)$&lt;/li&gt;
&lt;li&gt;Reverse KL Divergence: $\min_{q_ \theta} D_{\text{KL}}(q_ \theta \ \Vert \ p)$&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;ideal-cases&#34;&gt;Ideal Cases
&lt;/h3&gt;&lt;p&gt;In ideal scenario, we have&lt;/p&gt;
$$
\begin{gathered}
\min_{q_ \theta} D_{\text{KL}}(p \ \Vert \ q_ \theta) = 0 \ \Rightarrow \ q_ \theta^*(x) = p(x), \\
\min_{q_ \theta} D_{\text{KL}}(q_ \theta \ \Vert \ p) = 0 \ \Rightarrow \ q_ \theta^*(x) = p(x),
\end{gathered}
$$&lt;p&gt;which means that the optimal solution is the target distribution itself. Therefore, in this case, optimizing both Forward and Reverse KL Divergence are equivalent.&lt;/p&gt;
&lt;h3 id=&#34;practical-cases&#34;&gt;Practical Cases
&lt;/h3&gt;&lt;p&gt;In practice, the model distribution $q_\theta(x)$ is determined by the parameter $\theta$ (e.g., a single Gaussian distribution or a neural network with a specific architecture), and it constitutes a restricted hypothesis space $\mathcal{Q}_ \theta$. The true distribution $p(x)$, however, is often extremely complex and does not belong to the set $\mathcal{Q}_ \theta$ at all. This implies that $\min D_{\text{KL}} &amp;gt; 0$.&lt;/p&gt;
&lt;p&gt;In this case, the objective functions for the Forward and Reverse KL Divergence will cause the parameters $\theta^*$ to &lt;strong&gt;converge in completely different directions&lt;/strong&gt;. We will perform a rigorous derivation using the example of fitting an arbitrary complex distribution with a single Gaussian distribution.&lt;/p&gt;
&lt;p&gt;Suppose the target distribution $p(x)$ is an unknown complex multimodal distribution, and the model distribution is a Gaussian distribution $q_ \theta(x) = \mathcal{N}(x; \mu, \Sigma)$, with parameters $\theta = {\mu, \Sigma}$.&lt;/p&gt;
&lt;h4 id=&#34;forward-kl-divergence&#34;&gt;Forward KL Divergence
&lt;/h4&gt;&lt;p&gt;If we take the Forward KL Divergence as the training objective, we have&lt;/p&gt;
$$
\min_ \theta D_{\text{KL}}(p \ \Vert \ q_\theta) = \min_ \theta \mathbb{E}_{x \sim p(x)} \left[\log \frac{p(x)}{q_\theta(x)}\right] = \min_ \theta \mathbb{E}_{x \sim p(x)} [\log p(x) - \log q_\theta(x)],
$$&lt;p&gt;where minimizing $q_ \theta$ is equivalent to minimizing $\theta$ because $q_ \theta$ is completely determined by the parameter $\theta$. Since $\mathbb{E}_{x \sim p(x)}[\log p(x)]$ is constant w.r.t $\theta$, this is equivalent to &lt;strong&gt;Maximum Likelihood Estimation (MLE)&lt;/strong&gt;:&lt;/p&gt;
$$
\theta^* = \arg\max_\theta \mathbb{E}_{x \sim p(x)}[\log q_\theta(x)].
$$&lt;p&gt;Substituting the logarithm of the probability density of a single Gaussian distribution, $\log q_\theta(x) = -\frac{1}{2}(x-\mu)^T \Sigma^{-1} (x-\mu) - \frac{1}{2}\log\vert{}\Sigma\vert{} + C$, we have&lt;/p&gt;
$$
\mu^* = \arg\max_\mu \mathbb{E}_{x \sim p(x)}\left[ -\frac{1}{2}(x-\mu)^T \Sigma^{-1} (x-\mu) \right].
$$&lt;p&gt;Taking the derivative w.r.t. $\mu$ and setting it to zero, we have&lt;/p&gt;
$$
0 = \nabla_\mu \mathbb{E}_{x \sim p(x)} \left[ -\frac{1}{2}(x-\mu)^T \Sigma^{-1} (x-\mu) \right] = \mathbb{E}_{x \sim p(x)}[\Sigma^{-1}(x-\mu)].
$$&lt;p&gt;Since $\Sigma^{-1}$ is full rank, we have&lt;/p&gt;
$$
\mu^* = \mathbb{E}_{x \sim p(x)}[x].
$$&lt;p&gt;&lt;strong&gt;Conclusion&lt;/strong&gt;: The optimal mean is the &lt;strong&gt;global mean&lt;/strong&gt; of the true distribution, covering all modes. If the target distribution consists of two peaks that are very far apart (such as the distributions of cat and dog images), the model’s mean will be firmly locked at the physical center between these two peaks (which is often a meaningless “valley” region with no probability mass at all). This is why it is called &lt;strong&gt;Mode Covering&lt;/strong&gt; (also known as &lt;strong&gt;Mean-Seeking&lt;/strong&gt;, &lt;strong&gt;Zero-Avoiding&lt;/strong&gt;).&lt;/p&gt;
&lt;h4 id=&#34;reverse-kl-divergence&#34;&gt;Reverse KL Divergence
&lt;/h4&gt;&lt;p&gt;If we take the Reverse KL Divergence as the training objective, we have&lt;/p&gt;
$$
\min_ \theta D_{\text{KL}}(q_\theta \ \Vert \ p) = \min_ \theta \mathbb{E}_{x \sim q_\theta(x)} \left[\log \frac{q_\theta(x)}{p(x)}\right] = \min_ \theta \left[ -H(q_\theta(x)) + \mathbb{E}_{x \sim q_\theta(x)} \left[ -\log p(x) \right] \right].
$$&lt;p&gt;The objective function here consists of two parts, which is the essence of maximizing the ELBO in variational inference:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The cross-entropy term $\mathbb{E}_ {q_ \theta}[-\log p(x)]$: This requires that $q_\theta(x)$ be concentrated in regions where $p(x)$ is large. If $q_\theta$ allocates probability mass to the “valley” regions where $p(x) \approx 0$, causing $-\log p(x) \to \infty$, the penalty from this term will instantly explode.&lt;/li&gt;
&lt;li&gt;Negative entropy term $-H(q_ \theta)$: This requires that the variance of $q_\theta(x)$ itself be as small as possible (the more concentrated the distribution, the smaller the negative entropy).&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Conclusion&lt;/strong&gt;: The model collapses to a single mode to avoid low-density regions of $p(x)$. Its optimal solution is to select, from among the numerous peaks in $p(x)$, the local peak (mode) with the largest “area” and the easiest to fit, then place $\mu^*$ at the vertex of this peak and shrink the variance $\Sigma$ so that it is completely contained within this single peak. This is why it is called &lt;strong&gt;Mode-Seeking&lt;/strong&gt; (also known as &lt;strong&gt;Zero-Forcing&lt;/strong&gt;).&lt;/p&gt;
</description>
        </item>
        <item>
        <title>Study Notes on Stochastic Optimal Control</title>
        <link>https://serendipityerr.github.io/p/study-notes-on-stochastic-optimal-control/</link>
        <pubDate>Sat, 06 Dec 2025 00:19:33 +0800</pubDate>
        
        <guid>https://serendipityerr.github.io/p/study-notes-on-stochastic-optimal-control/</guid>
        <description>&lt;p&gt;Control theory is a mathematical description of how to act optimally to gain future rewards.&lt;/p&gt;
&lt;h2 id=&#34;deterministic-discrete-time-control&#34;&gt;Deterministic Discrete Time Control
&lt;/h2&gt;&lt;p&gt;We start by discussing the most simple control case, which is the finite horizon discrete time deterministic control problem.&lt;/p&gt;
&lt;p&gt;Consider the control of a discrete time dynamical system:&lt;/p&gt;
$$
x_{t+1} = x_{t} + f(t, x_t, u_t), \quad t=0,1,\cdots,T-1
$$&lt;p&gt;where $x_t$ is an $n$-dimensional vector describing the &lt;strong&gt;state&lt;/strong&gt; of the system and $u_t$ is an $m$-dimensional vector that specifies the &lt;strong&gt;control&lt;/strong&gt; or &lt;strong&gt;action&lt;/strong&gt; at time $t$. If we specify $x_0$ at $t = 0$ and we specify a sequence of controls $u_{0:T−1} = u_0, u_1, \cdots , u_{T−1}$, we can compute future states of the system $x_{1:T}$ recursively.&lt;/p&gt;
&lt;p&gt;Define the cost function $\mathcal{C}$ as:&lt;/p&gt;
$$
\mathcal{C}(x_0, u_{0:T-1}) = \sum_{t=0}^{T-1} R (x_t, u_t, t) + \phi(x_T) 
$$&lt;p&gt;where $R (t, x_t, u_t)$ is the cost that is associated with taking action $u_t$ at time $t$ in state $x_t$ (trajectory cost), and $\phi(x_T)$ is the cost associated with ending up in state $x_T$ at time $T$ (terminal cost). The problem of optimal control is to find the sequence $u_{0:T−1}$ that minimizes $\mathcal{C}(x_0, u_{0:T−1})$:&lt;/p&gt;
$$
\begin{gathered}
\min_{u_{0:T−1}} \mathcal{C}(x_0, u_{0:T−1}) \\
x_{t+1} = x_{t} + f(x_t, u_t, t), \ t=0,1,\cdots,T-1, \ x_{0} = x_{0}
\end{gathered}
$$&lt;p&gt;Now, we introduce the optimal &lt;strong&gt;cost to go&lt;/strong&gt; function $J$:&lt;/p&gt;
$$
J(x_t, t) = \min_{u_{t:T−1}} \left( \sum_{s=t}^{T-1} R (x_s, u_s, s) + \phi(x_T) \right)
$$&lt;p&gt;which solves the optimal control problem from an intermediate time $t$ until the fixed end time $T$ , starting at an arbitrary location $x_t$. We have&lt;/p&gt;
$$
\begin{gathered}
J(x_0, 0) = \min_{u_{0:T−1}} \mathcal{C}(x_0, u_{0:T−1})\\
J(x_T, T) = \phi(x_T) 
\end{gathered}
$$&lt;p&gt;Consider recursively computing $J(x_t, t)$ as&lt;/p&gt;
$$
\begin{aligned}
J(x_t, t) &amp;= \min_{u_{t:T−1}} \left( \sum_{s=t}^{T-1} R (x_s, u_s, s) + \phi(x_T) \right) \\
&amp;= \min_{u_{t}} \left[ R (x_t, u_t, t) + \min_{u_{t+1:T−1}} \left( \sum_{s=t+1}^{T-1} R (x_s, u_s, s) + \phi(x_T) \right) \right] \\
&amp;= \min_{u_{t}} \left[ R (x_t, u_t, t) + J(x_{t+1}, t+1) \right] \\
&amp;= \min_{u_{t}} \left[ R (x_t, u_t, t) + J(x_{t} + f(x_t, u_t, t), t+1) \right] 
\end{aligned}
$$&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; the minimization over the whole path $u_{t:T−1}$ has reduced to a sequence of minimizations over $u_t$. This simplification is due to the &lt;strong&gt;Markovian&lt;/strong&gt; nature of the problem: the future depends on the past and vise versa only through the present. The algorithm to compute the optimal control $u_{0:T−1}^{\ast}$, the optimal trajectory $x_{1:T}^{\ast}$ and the optimal cost is given by&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Initialize $J(x_T, T) = \phi(x_T)$&lt;/li&gt;
&lt;li&gt;Backwards: For $t=T-1, \cdots, 1, 0$, and For all $x_t$, compute&lt;/li&gt;
&lt;/ol&gt;
$$
\begin{gathered}
u^*_t(x_t) = \arg\min_u \{ R(x_t, u, t) + J(t+1, x_t + f(x_t, u, t)) \} \\
J(t, x_t) = R(x_t, u^*_t(x_t), t) + J(t+1, x_t + f(x_t, u^*_t(x_t), t)
\end{gathered}
$$&lt;ol start=&#34;3&#34;&gt;
&lt;li&gt;Forwards: For $t=T-1, \cdots, 1, 0$ compute&lt;/li&gt;
&lt;/ol&gt;
$$
x^*_{t+1} = x^*_{t} + f(x^*_{t}, u^*_{t}, t), \quad x^*_0 = x_0
$$&lt;p&gt;This is just like &lt;strong&gt;Dynamic Programming (DP)&lt;/strong&gt; algorithm. It is linear in the horizon time $T$ and linear in the size of the state and action spaces.&lt;/p&gt;
&lt;h2 id=&#34;deterministic-continuous-time-control&#34;&gt;Deterministic Continuous Time Control
&lt;/h2&gt;&lt;p&gt;Now, consider the optimal control problem in continuous time:&lt;/p&gt;
$$
x_{t+\mathrm{d}t} = x_{t} + f(x_t, u_t, t)\mathrm{d}t, \quad \mathrm{d}x_t = f(x_t, u_t, t)\mathrm{d}t
$$&lt;p&gt;We have two methods for solving the optimal control problem: 1) Pontryagin Minimum Principle (PMP) (a pair of ordinary differential equations); 2) Hamilton-Jacobi-Bellman (HJB) equation (a partial differential equation).&lt;/p&gt;
&lt;h3 id=&#34;hamilton-jacobi-bellman-equation&#34;&gt;Hamilton-Jacobi-Bellman Equation
&lt;/h3&gt;&lt;p&gt;The initial state is fixed: $x_0 = x_0$ and the final state is free. The problem is to find a control signal $u_t, 0 &amp;lt; t &amp;lt; T$ , which we denote as $u(0 \rightarrow T)$, such that minimizing&lt;/p&gt;
$$
C(x_0, u(0 \rightarrow T)) = \int_0^T R(x_t, u_t, t) \mathrm{d} t + \phi(x_T)
$$&lt;p&gt;Then, the &lt;em&gt;cost to go&lt;/em&gt; function can be extended into&lt;/p&gt;
$$
J(x_t, t) = \min_{u_{t \rightarrow T}} \left( \int_{t}^{T} R (x_z, u_z, z) \mathrm{d} z + \phi(x_T) \right) 
$$&lt;p&gt;Again consider recursively computing $J(x_t, t)$ as&lt;/p&gt;
$$
\begin{aligned}
J(x_t, t) &amp;= \min_{u_{t}} \left( R (x_t, u_t, t) \mathrm{d} t + \min_{u_{t+\mathrm{d}t \rightarrow T}} \left( \int_{t+\mathrm{d} t}^{T} R (x_z, u_z, z) \mathrm{d} z + \phi(x_T)\right) \right) \\
&amp;= \min_{u_{t}} \left( R (x_t, u_t, t) \mathrm{d} t + J(x_{t+\mathrm{d}t}, t+\mathrm{d}t) \right)  \\
&amp;\approx \min_{u_{t}} \left( R (x_t, u_t, t) \mathrm{d} t + J(x_t, t) + \partial_t J(x_{t}, t) \mathrm{d} t + \partial_{x_t}J(x_{t}, t)f(x_t, u_t, t) \mathrm{d} t \right) 
\end{aligned}
$$&lt;p&gt;which implies&lt;/p&gt;
$$
-\partial_t J(x_{t}, t) = \min_{u_{t}} \left( R (x_t, u_t, t) + f(x_t, u_t, t) \partial_{x_t}J(x_{t}, t) \right)
$$&lt;p&gt;which is a partial differential equation, known as the &lt;strong&gt;Hamilton-Jacobi-Bellman (HJB)&lt;/strong&gt; equation that describes the evolution of $J$ as a function of $x$ and $t$ and must be solved with boundary condition $J(x_T, T) = \phi(x)$. $\partial_t$ and $\partial_x$ denote partial derivatives with respect to $t$ and $x$, respectively.&lt;/p&gt;
&lt;p&gt;The optimal control $u_t(x_t)$ at the current $x_t$, $t$ is given by&lt;/p&gt;
$$
u_t(x_t) = \arg \min_{u} \left( R(x_t, u, t) + \partial_{x_t}J(x_{t}, t) f(x_t, u, t) \right)
$$&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; in order to compute the optimal control at the initial state $x_0$ at $t = 0$, one must compute $J(x_t, t)$ for all values of $x_t$ and $t$.&lt;/p&gt;
&lt;h3 id=&#34;pontryagin-minimum-principle&#34;&gt;Pontryagin Minimum Principle
&lt;/h3&gt;&lt;p&gt;An alternative approach is a variational method that directly finds the optimal trajectory and optimal control and bypasses the expensive computation of the cost-to-go. We can write the optimal control problem as a constrained optimization problem:&lt;/p&gt;
$$
\begin{gathered}
\min_{u_t} \int_0^T R(x_t, u_t, t) \mathrm{d} t + \phi(x_T) \\
\text{s.t.} \ \mathrm{d} x_t = f(x_t, u_t, t) \mathrm{d} t, \ x_0 = x_0
\end{gathered}
$$&lt;p&gt;We can solve the constraint optimization problem by introducing the Lagrange multiplier function $\lambda_t$ that ensures the ODE constraint for all $t$:&lt;/p&gt;
$$
\begin{aligned}
\mathcal{C} &amp;= \int_0^T \left[R(x_t, u_t, t) - \lambda_t \left(f(x_t, u_t, t) - \frac{\mathrm{d} x_t}{\mathrm{d} t} \right) \right] \mathrm{d} t + \phi(x_T) \\
&amp;= \int_0^T \left[-H(x_t, u_t, \lambda_t, t) + \lambda_t \frac{\mathrm{d} x_t}{\mathrm{d} t} \right] \mathrm{d} t + \phi(x_T) \\
\end{aligned}
$$&lt;p&gt;where we defined $H(x_t, u_t, \lambda_t, t) = -R(x_t, u_t, t) + \lambda_t f(x_t, u_t, t)$. The solution is found by extremizing $\mathcal{C}$. We can find the extremal solution if we compute the change $\delta \mathcal{C}$ in $\mathcal{C}$ that results from independent variation of $x_t$, $u_t$, and $\lambda_t$ at all times $t$ between $0$ and $T$. We denote these variations by $\delta x_t$, $\delta u_t$, and $\delta \lambda_t$, respectively. We have:&lt;/p&gt;
$$
\delta \mathcal{C} = \int_0^T \left[-\partial_{x_t} H \delta x_t - \partial_{u_t} H \delta u_t + \left(-\partial_{\lambda_t} H + \frac{\mathrm{d} x_t}{\mathrm{d} t}\right) \delta \lambda_t + \lambda_t \delta \frac{\mathrm{d} x_t}{\mathrm{d} t} \right] \mathrm{d} t + \partial_{x_T} \phi(x_T) \delta \phi(x_T) 
$$&lt;p&gt;Take partial integration:&lt;/p&gt;
$$
\int_0^T \lambda_t \delta \frac{\mathrm{d} x_t}{\mathrm{d} t} \mathrm{d} t = \int_0^T \lambda_t \frac{\mathrm{d}\delta x_t}{\mathrm{d} t} \mathrm{d} t = -\int_0^T \delta x_t \frac{\mathrm{d} \lambda_t}{\mathrm{d} t} \mathrm{d} t + \lambda_T \delta x_T - \lambda_0 \delta x_0
$$&lt;p&gt;and $\delta x_0 = 0$ as $x_0$ is given.&lt;/p&gt;
&lt;p&gt;The stationary solution $\delta \mathcal{C} = 0$ is obtained when the coefficients of the independent variations $\delta x_t$, $\delta u_t$, $\delta \lambda_t$, and $\delta x_T$ are zero. Thus,&lt;/p&gt;
$$
\begin{aligned}
0 &amp;= \partial_{u_t} H(x_t, u_t, \lambda_t, t) \\
\frac{\mathrm{d} \lambda_t}{\mathrm{d} t} &amp;= - \partial_{x_t} H(x_t, u_t, \lambda_t, t) \\
\frac{\mathrm{d} x_t}{\mathrm{d} t} &amp;= \partial_{\lambda_t} H(x_t, u_t, \lambda_t, t) \\
\lambda_T &amp;= - \partial_{x_T} \phi(x_T)
\end{aligned}
$$&lt;p&gt;The first ODE implies the optimal controller $u_t^*(x_t, \lambda_t, t)$. The remaining ODEs are:&lt;/p&gt;
$$
\begin{aligned}
\frac{\mathrm{d} \lambda_t}{\mathrm{d} t} &amp;= - \partial_{x_t} H(x_t, u_t^*, \lambda_t, t) \\
\frac{\mathrm{d} x_t}{\mathrm{d} t} &amp;= \partial_{\lambda_t} H(x_t, u_t^*, \lambda_t, t)
\end{aligned}
$$&lt;p&gt;with the boundary conditions $x_0 = x_0$ and $\lambda_T = - \partial_{x_T} \phi(x_T)$. There remains two coupled ODEs that describe the dynamics of $x_t$ and $\lambda_t$ over time with boundary conditions.&lt;/p&gt;
&lt;h2 id=&#34;stochastic-optimal-control&#34;&gt;Stochastic Optimal Control
&lt;/h2&gt;&lt;p&gt;In this section, we consider the extension of the continuous control problem to the case that the dynamics is subject to noise and is given by a Stochastic Differential Equation (SDE).&lt;/p&gt;
&lt;p&gt;Consider the following controlled SDE:&lt;/p&gt;
$$
\mathrm{d}x_t = f(x_t, u_t, t) \mathrm{d} t + g_t \mathrm{d}w_t,
$$&lt;p&gt;where $f: \mathbb{R}^d \times \mathbb{R}^d \times [0, T] \rightarrow \mathbb{R}^d$ is the vector-valued drift function, $g:[0, T] \rightarrow \mathbb{R}$ signifies the scalar-valued diffusion coefficient and $w_t \in \mathbb{R}^d$ is the standard Wiener process, also known as Brownian motion.&lt;/p&gt;
&lt;p&gt;Since the dynamics is stochastic, it is no longer the case that when $x$ at time $t$ and the full control path $u(t \rightarrow T)$ are given, we know the future path $x(t \rightarrow T)$. Therefore, we could only hope to be able to minimize its expectation value over all possible future realizations of the Wiener process:&lt;/p&gt;
$$
\mathcal{C}(x_0, u(0 \rightarrow T)) = \mathbb{E}_{x_0} \left[ \int_0^T R(x_t, u_t, t) \mathrm{d} t + \phi(x_T) \right]
$$&lt;p&gt;where the subscript $x_0$ on the expectation value is to remind us that the expectation is over all stochastic trajectories that start in $x_0$. The solution of the control problem proceeds very similar as in the deterministic case: the &lt;em&gt;cost to go&lt;/em&gt; function can be extended into&lt;/p&gt;
$$
J(x_t, t) = \min_{u_{t \rightarrow T}} \mathbb{E}_{x_t} \left[ \int_{t}^{T} R (x_z, u_z, z) \mathrm{d} z + \phi(x_T) \right] 
$$&lt;p&gt;Again consider recursively computing $J(x_t, t)$ as&lt;/p&gt;
$$
\begin{aligned}
J(x_t, t) &amp;= \min_{u_{t}} \mathbb{E}_{x_t} \left[ R (x_t, u_t, t) \mathrm{d} t + \min_{u_{t+\mathrm{d}t \rightarrow T}} \left( \int_{t+\mathrm{d} t}^{T} R (x_z, u_z, z) \mathrm{d} z  + \phi(x_T)\right) \right] \\
&amp;= \min_{u_{t}} \left( R (x_t, u_t, t) \mathrm{d} t + \mathbb{E}_{x_t} \left[ J(x_{t+\mathrm{d}t}, t+\mathrm{d}t) \right] \right)
\end{aligned}
$$&lt;p&gt;Again make a Taylor expansion:&lt;/p&gt;
$$
\begin{aligned}
\mathbb{E}_{x_t} \left[ J(x_{t+\mathrm{d}t}, t+\mathrm{d}t) \right] &amp;= \int \mathcal{N}(x_{t+\mathrm{d}t} \mid x_t) J(x_{t+\mathrm{d}t}, t+\mathrm{d}t) \mathrm{d} x_{t+\mathrm{d}t} \\
&amp;\approx J(x_{t}, t) + \partial_t J(x_{t}, t) \mathrm{d}t + \partial_{x_t} J(x_{t}, t) \langle \mathrm{d}x_t \rangle + \frac{1}{2} \partial^2_{x_t} J(x_{t}, t) \langle \mathrm{d}x_t^2 \rangle \\
\end{aligned}
$$&lt;p&gt;where we have $\langle \mathrm{d}x_t \rangle = f(x_t, u_t, t) \mathrm{d} t$ and $\langle \mathrm{d}x_t^2 \rangle = g_t \mathrm{d} t$. Therefore,&lt;/p&gt;
$$
-\partial_t J(x_t, t) = \min_{u_{t}} \left( R (x_t, u_t, t) + f(x_t, u_t, t) \partial_{x_t}J(x_{t}, t) + \frac{g_t^2}{2} \partial^2_{x_t} J(x_{t}, t) \right)
$$&lt;p&gt;We can easily find that when $g_t \rightarrow 0$, the equation above would degrade to the deterministic one as we discussed in the &lt;a class=&#34;link&#34; href=&#34;#deterministic-continuous-time-control&#34; &gt;Section&lt;/a&gt; above (Deterministic Continuous Time Control).&lt;/p&gt;
&lt;h3 id=&#34;girsanov-theorem-and-measure-transformation&#34;&gt;Girsanov Theorem and Measure Transformation
&lt;/h3&gt;&lt;p&gt;In this section, we consider the transformation of probability measures for continuous-time stochastic processes. This theorem provides the mathematical foundation for connecting uncontrolled random dynamics with controlled dynamics, which is crucial for Path Integral Control.&lt;/p&gt;
&lt;p&gt;Consider a base stochastic dynamics (uncontrolled process) defined by a Stochastic Differential Equation (SDE) under the probability measure $\mathbb{P}$:&lt;/p&gt;
$$
\mathrm{d}x_t = f(x_t, t) \mathrm{d}t + g_t \mathrm{d}w_t^{\mathbb{P}},
$$&lt;p&gt;where $w_t^{\mathbb{P}}$ is the standard Wiener process under measure $\mathbb{P}$.&lt;/p&gt;
&lt;p&gt;Now, assume we apply an external control $u_t$ to the system, altering its drift. The controlled dynamics under a new target measure $\mathbb{Q}$ is given by:&lt;/p&gt;
$$
\mathrm{d}x_t = \left( f(x_t, t) + g_t u_t \right) \mathrm{d}t + g_t \mathrm{d}w_t^{\mathbb{Q}}
$$&lt;p&gt;where $w_t^{\mathbb{Q}}$ is the standard Wiener process under the new measure $\mathbb{Q}$. Notice that the diffusion coefficient $g_t$ must remain identical for the two measures to be equivalent (mutually absolutely continuous).&lt;/p&gt;
&lt;p&gt;Girsanov Theorem states that the Radon-Nikodym derivative (the probability density ratio of the path measures) between the controlled universe $\mathbb{Q}$ and the uncontrolled universe $\mathbb{P}$ over the time interval $[0, T]$ is given by:&lt;/p&gt;
$$
\frac{\mathrm{d}\mathbb{Q}}{\mathrm{d}\mathbb{P}} = \exp\left( \int_0^T u_t^\top \mathrm{d}w_t^{\mathbb{P}} - \frac{1}{2} \int_0^T \|u_t\|^2 \mathrm{d}t \right)
$$&lt;p&gt;Since the path distributions are different, we are often interested in the information-theoretic distance between them, namely the Kullback-Leibler (KL) divergence. We evaluate the expectation of the log-likelihood ratio under the controlled measure $\mathbb{Q}$:&lt;/p&gt;
$$
D_{KL}(\mathbb{Q} \parallel \mathbb{P}) = \mathbb{E}_{\mathbb{Q}} \left[ \log \frac{\mathrm{d}\mathbb{Q}}{\mathrm{d}\mathbb{P}} \right]
$$&lt;p&gt;To compute this, we first substitute the relationship between the Wiener processes under the two measures, $\mathrm{d}w_t^{\mathbb{P}} = \mathrm{d}w_t^{\mathbb{Q}} + u_t \mathrm{d}t$, into the integral:&lt;/p&gt;
$$
\begin{aligned}
\mathbb{E}_{\mathbb{Q}} \left[ \log \frac{\mathrm{d}\mathbb{Q}}{\mathrm{d}\mathbb{P}} \right] &amp;= \mathbb{E}_{\mathbb{Q}} \left[ \int_0^T u_t^\top \left( \mathrm{d}w_t^{\mathbb{Q}} + u_t \mathrm{d}t \right) - \frac{1}{2} \int_0^T \|u_t\|^2 \mathrm{d}t \right] \\
&amp;= \mathbb{E}_{\mathbb{Q}} \left[ \int_0^T u_t^\top \mathrm{d}w_t^{\mathbb{Q}} + \int_0^T \|u_t\|^2 \mathrm{d}t - \frac{1}{2} \int_0^T \|u_t\|^2 \mathrm{d}t \right] \\
&amp;= \mathbb{E}_{\mathbb{Q}} \left[ \int_0^T u_t^\top \mathrm{d}w_t^{\mathbb{Q}} \right] + \mathbb{E}_{\mathbb{Q}} \left[ \int_0^T \frac{1}{2} \|u_t\|^2 \mathrm{d}t \right]
\end{aligned}
$$&lt;p&gt;A fundamental property of the Itô integral is that the integral with respect to Brownian motion $\int u_t^\top \mathrm{d}w_t^{\mathbb{Q}}$ is a martingale under the measure $\mathbb{Q}$, meaning its expectation evaluates to zero:&lt;/p&gt;
$$
\mathbb{E}_{\mathbb{Q}} \left[ \int_0^T u_t^\top \mathrm{d}w_t^{\mathbb{Q}} \right] = 0
$$&lt;p&gt;Therefore, the equation elegantly simplifies to:&lt;/p&gt;
$$
D_{KL}(\mathbb{Q} \parallel \mathbb{P}) = \mathbb{E}_{\mathbb{Q}} \left[ \int_0^T \frac{1}{2} \|u_t\|^2 \mathrm{d}t \right].
$$&lt;p&gt;We can easily find that the KL divergence (information-theoretic distance) between the controlled and uncontrolled path measures is exactly equivalent to the cumulative quadratic control cost. This perfectly explains why minimizing the control effort in Stochastic Optimal Control is mathematically equivalent to minimizing the discrepancy between probability distributions.&lt;/p&gt;
</description>
        </item>
        
    </channel>
</rss>
